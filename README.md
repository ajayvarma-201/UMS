# pip install streamlit

streamlit run main.py

# Uploading the project on GITHUB
# step-1 : Create a repository on GITHUB
# step-2 : in gitbasg change to your project directory : cd
# step-3 : clone the repository

#  git clone URL_of_repository

# step-4 : Copy the main.py & requirments.txt inside the local repo

# step-5 : cd repo folder

# step-6 : Run below commands in gitbash
git status
git add .
git commit -m "Project uploaded successfully"
git push